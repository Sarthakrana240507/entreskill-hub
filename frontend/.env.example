# Base URL of the backend API (Express server), including the /api/v1 prefix.
# Local development:
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1

# Production (set this to your deployed backend URL, e.g. Render/Railway):
# NEXT_PUBLIC_API_URL=https://entreskill-hub-api.onrender.com/api/v1
